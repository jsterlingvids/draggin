import { ImageCallback } from '@jimp/plugin-dither/node_modules/@jimp/core';

interface Dither {
  dither565(cb?: ImageCallback<this>): this;
  dither16(cb?: ImageCallback<this>): this;
}

export default function(): Dither;
