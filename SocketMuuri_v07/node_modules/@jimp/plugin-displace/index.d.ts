import { Jimp, ImageCallback } from '@jimp/plugin-displace/node_modules/@jimp/core';

interface Displace {
  displace(map: Jimp, offset: number, cb?: ImageCallback<this>): this;
}

export default function(): Displace;
