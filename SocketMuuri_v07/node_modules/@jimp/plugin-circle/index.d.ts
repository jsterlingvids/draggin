import { ImageCallback } from '@jimp/plugin-circle/node_modules/@jimp/core';

interface Circle {
  circle(options?: {
    radius: number,
    x: number,
    y: number
  }, cb?: ImageCallback<this>): this;
  circle(cb?: ImageCallback<this>): this;
}

export default function(): Circle;
